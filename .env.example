# Google Authentication Configuration
# Get these values from Google Cloud Console (https://console.cloud.google.com/)
# Create a new project or use existing one
# Enable Google Sign-In API
# Create OAuth 2.0 credentials

# Web Client ID (required for both iOS and Android)
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your-google-web-client-id.apps.googleusercontent.com

# iOS Client ID (only needed for iOS)
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=your-google-ios-client-id.apps.googleusercontent.com

# Client Secret (only needed for web authentication flow)
EXPO_PUBLIC_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Apple Authentication Configuration
# Apple Sign-In is configured automatically for iOS
# For web support, you'll need to configure Sign in with Apple JS
# More info: https://developer.apple.com/documentation/sign_in_with_apple/sign_in_with_apple_js

# Development Configuration
# Set to true for development mode
EXPO_PUBLIC_DEV_MODE=true

# Instructions for Setup:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Google OAuth credentials
# 3. For Google: Go to Google Cloud Console → APIs & Services → Credentials
# 4. For Apple: Configure in Apple Developer Console and Expo
# 5. Never commit the .env file to version control